name: Visual Pack submission
description: Share a new Visual Pack or publish an update to one of your existing Visual Packs.
title: "[Visual Pack] "
labels:
  - visual-pack-submission
body:
  - type: markdown
    attributes:
      value: |
        Thanks for sharing your Visual Pack! Use **Share Community Pack** in Aniki Pack Creator and attach the prepared file(s) below.

        GitHub limits individual ZIP attachments to 25 MB. If your pack is too large, Aniki Pack Creator automatically creates several `.partXX-of-XX.zip` files. **Attach every generated part and do not rename or modify them.**

        If this is an update, reopen the same Creator project and increase its version so the permanent pack ID stays unchanged.
  - type: input
    id: pack-name
    attributes:
      label: Pack name
      placeholder: Tekken 8
    validations:
      required: true

  - type: input
    id: author
    attributes:
      label: Author
      description: Name displayed to users in the Community Packs browser.
      placeholder: Mike Aniki
    validations:
      required: true
  - type: input
    id: version
    attributes:
      label: Version
      description: Use the exact version exported by Aniki Pack Creator.
      placeholder: 1.0.0
    validations:
      required: true

  - type: textarea
    id: description
    attributes:
      label: Description
      description: Optional short description shown to users.
      placeholder: Visual Pack inspired by Tekken 8.
  - type: textarea
    id: zip
    attributes:
      label: Visual Pack ZIP
      description: Attach the ZIP prepared by Aniki Pack Creator. If the Creator generated multipart ZIPs, attach ALL `.partXX-of-XX.zip` files here.
      placeholder: Attach the ZIP, or all generated multipart ZIPs, here.
    validations:
      required: true

  - type: textarea
    id: preview
    attributes:
      label: Preview image
      description: Attach one representative JPG or PNG image that users will see in the Community browser.
      placeholder: Attach the preview image here.
    validations:
      required: true
  - type: dropdown
    id: submission-type
    attributes:
      label: Submission type
      options:
        - New pack
        - Update to an existing pack
    validations:
      required: true
  - type: checkboxes
    id: confirmations
    attributes:
      label: Confirmation
      options:
        - label: I confirm the ZIP or multipart ZIPs were prepared directly with Aniki Pack Creator.
          required: true
        - label: I understand that updates must keep the permanent ID from the original Creator project.
          required: true
        - label: I am responsible for the content I submit and have the right to share it.
          required: true
